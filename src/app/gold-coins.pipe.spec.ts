import { GoldCoinsPipe } from './gold-coins.pipe';

describe('GoldCoinsPipe', () => {
  it('create an instance', () => {
    const pipe = new GoldCoinsPipe();
    expect(pipe).toBeTruthy();
  });
});
